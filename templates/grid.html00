<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>grid</title>
</head>
<body>
{{range .}}
<div class="profile-card">
    <img src="{{ .ProfilePicURL }}" alt="{{ .Username }}" loading=lazy>
    <h3>{{ .Username }}</h3>
</div>
{{end}}
</body>
</html>